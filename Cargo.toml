[package]
name = "anticipate"
version = "0.1.0"
edition = "2021"
description = "Script runner for rexpect with asciinema support"

[workspace]
members = [ "compiler",
  "parser",
  "compiler",
]

[dependencies]
rexpect = "0.5"
